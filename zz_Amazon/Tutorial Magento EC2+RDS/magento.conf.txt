server {
    server_name homolog.mage2b.com ;

    root /usr/share/nginx/html/magento;
    index index.php;

    #access_log /usr/share/nginx/html/magento2/homolog.access_log main;
    #error_log /usr/share/nginx/html/magento2/homolog.error_log info;

    if (!-e $request_filename) {
        rewrite /wp-admin$ $scheme://$host$uri/ permanent;
        rewrite ^(/[^/]+)?(/wp-.*) $2 last;
        rewrite ^(/[^/]+)?(/.*\.php) $2 last;
    }

    location / {
        try_files $uri $uri/ /index.php?$args ;
    }

    location ~ \.php$ {
        try_files $uri =404;
        include fastcgi_params;
        #fastcgi_pass php;
        fastcgi_pass    unix:/var/run/php/php7.0-fpm.sock;
        fastcgi_index  index.php;
        fastcgi_param  SCRIPT_FILENAME  /usr/share/nginx/html/magento$fastcgi_script_name;
        include        fastcgi_params;
    }

    #add some rules for static content expiry-headers here
}
